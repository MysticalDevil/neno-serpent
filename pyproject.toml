[project]
name = "nenoserpent-tools"
version = "0.1.0"
description = "Developer tooling environment for NenoSerpent scripts"
requires-python = ">=3.13"
dependencies = [
    "numpy>=2.4.2",
    "torch>=2.10.0",
]

[tool.uv]
package = false
